<view class="container" wx:if="{{!loading}}">
  <view class="header text-center anim-fade-in">
    <text class="title font-song">落款 · 复盘</text>
    <view class="divider"></view>
  </view>
  
  <scroll-view scroll-y class="content anim-fade-in" style="animation-delay: 0.2s;">
    <!-- 评分卡片 -->
    <paper-card className="score-card">
      <view class="score-circle">
        <text class="score-val font-song text-red">{{score}}</text>
        <text class="score-total font-sans text-gray">/100</text>
      </view>
      <view class="score-label font-song">本篇章完成度</view>
      
      <view class="stats-row font-sans text-gray">
        <text>完成 {{stats.completed}} 天</text>
        <text class="divider">|</text>
        <text>留白 {{stats.skipped}} 天</text>
      </view>

      <view class="summary-text font-song text-ink">
        {{summary}}
      </view>

      <!-- 盖章动画 -->
      <view class="seal-anim {{showSeal ? 'show' : ''}}">
        <view class="seal-text font-song">{{score >= 80 ? '上吉' : (score >= 60 ? '中平' : '努力')}}</view>
      </view>
    </paper-card>

    <!-- 心得回顾列表 -->
    <view class="review-section" wx:if="{{notes.length > 0}}">
      <view class="section-title font-song">
        <text class="icon">❝</text> 拾遗
      </view>
      
      <view class="note-list">
        <view class="note-item" wx:for="{{notes}}" wx:key="day">
          <view class="note-day font-song text-gray">Day {{item.day}}</view>
          <view class="note-text font-sans">{{item.content}}</view>
        </view>
      </view>
    </view>
    
    <view class="empty-notes font-sans text-gray" wx:else>
      <text>本篇章暂无心得记录，下个旅程多留些墨迹吧。</text>
    </view>

  </scroll-view>

  <view class="footer anim-fade-in" style="animation-delay: 0.4s;">
    <ink-btn type="primary" bindtap="nextQuest">归档 · 开启新篇</ink-btn>
  </view>
</view>
